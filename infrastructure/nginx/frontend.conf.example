# Whistle Frontend Apps - Nginx Configuration
# Adapt for each subdomain (devs, earn, fun)

# Developer Showcase
server {
    listen 443 ssl http2;
    server_name devs.whistle.ninja;

    ssl_certificate /etc/letsencrypt/live/devs.whistle.ninja/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/devs.whistle.ninja/privkey.pem;

    root /var/www/devs.whistle.ninja;
    index index.html;

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;

    # API proxy
    location /api/ {
        proxy_pass http://localhost:3490/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # Static files
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Cache static assets
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2)$ {
        expires 1M;
        add_header Cache-Control "public, immutable";
    }
}

# Node Operator Dashboard
server {
    listen 443 ssl http2;
    server_name earn.whistle.ninja;

    ssl_certificate /etc/letsencrypt/live/earn.whistle.ninja/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/earn.whistle.ninja/privkey.pem;

    root /var/www/earn.whistle.ninja;
    index index.html;

    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;

    location / {
        try_files $uri $uri/ =404;
    }
}

# TX Race Game
server {
    listen 443 ssl http2;
    server_name fun.whistle.ninja;

    ssl_certificate /etc/letsencrypt/live/fun.whistle.ninja/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/fun.whistle.ninja/privkey.pem;

    root /var/www/fun.whistle.ninja;
    index index.html;

    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;

    # Profiles API
    location /api/ {
        proxy_pass http://localhost:3491/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}

